version: '3.8'

services:
  postgres:
    image: postgres:13
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:alpine
    restart: always

  osrm:
    image: ghcr.io/project-osrm/osrm-backend
    command: osrm-routed --algorithm mld /data/map.osrm
    volumes:
      ./data/regions/pl_wielkopolskie:/data

  django:
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      ./data:/data
    ports:
      - "8000:8000"
    depends_on:
      - osrm
      - postgres
      - redis
    env_file:
      - .env
    environment:
      OSRM_URL_pl_wielkopolskie: "http://osrm:5000"

volumes:
  postgres_data:
